import React from 'react';
import { CheckCircle2, CloudLightning } from 'lucide-react';
import { motion } from 'framer-motion';

export const BottomStatusBar: React.FC = () => {
    return (
        <footer className="h-8 border-t border-white/5 bg-background/95 backdrop-blur flex items-center justify-between px-4 text-xs text-muted-foreground font-mono z-20">
            <div className="flex items-center gap-4">
                <motion.span
                    initial={{ opacity: 0 }}
                    animate={{ opacity: 1 }}
                    className="flex items-center gap-1.5"
                >
                    <CheckCircle2 className="w-3.5 h-3.5 text-emerald-400" />
                    <span className="text-foreground/70">All systems operational</span>
                </motion.span>
                <div className="w-px h-3 bg-white/10"></div>
                <span className="px-2 py-0.5 bg-emerald-500/10 text-emerald-400 rounded border border-emerald-500/20 flex items-center shadow-[0_0_10px_rgba(16,185,129,0.1)]">
                    <span className="w-1.5 h-1.5 rounded-full bg-emerald-400 mr-1.5 animate-pulse"></span>
                    Production Alias Active
                </span>
            </div>

            <div className="flex items-center gap-4">
                <span className="flex items-center gap-1.5 hover:text-foreground cursor-pointer transition-colors group">
                    <CloudLightning className="w-3.5 h-3.5 text-amber-400 group-hover:drop-shadow-[0_0_5px_rgba(251,191,36,0.5)] transition-all" />
                    Eval Engine Ready
                </span>
                <div className="w-px h-3 bg-white/10"></div>
                <span className="opacity-50 hover:opacity-100 transition-opacity">PromptOps v1.2</span>
            </div>
        </footer>
    );
};
