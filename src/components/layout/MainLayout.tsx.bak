import React from 'react';
import { LeftConfigSidebar } from './LeftConfigSidebar';
import { CenterMessageEditor } from './CenterMessageEditor';
import { RightOutputPanel } from './RightOutputPanel';
import { TopToolbar } from './TopToolbar';
import { BottomStatusBar } from './BottomStatusBar';

export const MainLayout: React.FC = () => {
    return (
        <div className="flex flex-col h-screen w-screen overflow-hidden bg-[#0A0A0A] bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.15),rgba(255,255,255,0))] text-foreground font-sans selection:bg-blue-500/30">
            {/* Top Toolbar */}
            <TopToolbar />

            {/* Main 3-Panel Split Area */}
            <div className="flex-1 flex min-h-0 relative">
                <LeftConfigSidebar />
                <CenterMessageEditor />
                <RightOutputPanel />
            </div>

            {/* Bottom Status Bar */}
            <BottomStatusBar />
        </div>
    );
};

